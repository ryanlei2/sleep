import React from 'react'


//take array of results classes after survey data analyzed and put in firestore, then grab all data as array with title survey + i like survey1, survey2, etc
//dropdown to select prev schedule
//and if a schedule is selected, grab the selected classes from the survey page dropdown and put out a lil list on dashboard going into detail on the classes
export default function saveSurveyHistory() {
  return (
    <div>saveSurveyHistory</div>
  )
}
